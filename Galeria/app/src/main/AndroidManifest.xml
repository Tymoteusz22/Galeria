<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.galeria">

    <uses-sdk
        android:minSdkVersion="29"
        tools:ignore="GradleOverrides" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon">

        <activity android:name=".Screen.DirectoriesScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Screen.AllMediaScreen" />
        <activity android:name=".Screen.DirectoryContentScreen" />
        <activity android:name=".Screen.SimilarImagesScreen" />
        <activity android:name=".Screen.FullScreen" />
        <activity android:name=".Screen.SettingsScreen" />
        <activity android:name=".Screen.GraphicsScreen" />
    </application>

</manifest>