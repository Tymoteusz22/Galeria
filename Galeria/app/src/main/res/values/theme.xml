<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="GalleryThemeDay" parent="Theme.AppCompat">

        <!-- text size -->
        <!-- <item name="android:textSize">20sp</item> menu text size -->
        <!-- text colors -->
        <item name="android:textColor">@color/aqua</item> <!-- default in-app text color -->
        <item name="android:textColorPrimary">@color/aqua</item> <!-- actionbar text color -->
        <item name="android:textColorHint">@color/red</item> <!-- hint text color -->
        <!-- bg colors -->
        <item name="android:statusBarColor">@color/navy</item> <!-- statusBar color -->
        <item name="android:colorButtonNormal">@color/red</item> <!-- button bg color -->
        <item name="colorControlNormal">@color/black</item> <!-- checkbox color -->
        <item name="android:navigationBarColor">@color/olive</item>
        <item name="android:navigationBarDividerColor">@color/black</item>
    </style>
</resources>